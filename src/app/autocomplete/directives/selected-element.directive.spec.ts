import { SelectedElementDirective } from './selected-element.directive';

describe('SelectedElementDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedElementDirective();
    expect(directive).toBeTruthy();
  });
});
