import { FilterIfDirective } from './filter-if.directive';

describe('FilterIfDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterIfDirective();
    expect(directive).toBeTruthy();
  });
});
